#ifndef VIDEO_H
#define VIDEO_H

#include "image.cuh"

#include <string>

void decodeVideo(std::string filepath, void (*callback)(uchar3*, int2));
void decodeFramebuffer(std::string filepath, void (*callback)(Framebuffer));

#endif